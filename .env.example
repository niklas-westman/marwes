# Local-only Figma Personal Access Token for /figma-sync remote
# Copy this file to .env.local and replace the placeholder value.
# Required Figma scope: file_content:read
FIGMA_API_TOKEN="figd_replace_me"
