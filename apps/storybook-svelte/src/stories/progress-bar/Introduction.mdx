import { Meta } from "@storybook/addon-docs/blocks"

<Meta title="ProgressBar/Introduction" />

# ProgressBar Component

`ProgressBar` is a determinate progress indicator, not an input. Use `Slider` or `SliderField` when a user should adjust the value.

It supports visible label text, visible percentage text, small and default Figma track sizes, and `role="progressbar"` semantics.
