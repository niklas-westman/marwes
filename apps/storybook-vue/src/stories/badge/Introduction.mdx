import { Meta } from "@storybook/addon-docs/blocks"

<Meta title="Badge/Introduction" />

# Badge Components

Badges communicate compact status and metadata in a low-footprint UI element.

## Component Architecture

```
Badge (Atom)
```

## Recommended Usage

Use `Badge` for short, meaningful labels such as state, category, or severity.

```vue
<script setup lang="ts">
import { Badge } from "@marwes-ui/vue"
</script>

<template>
  <Badge variant="success">Active</Badge>
</template>
```

## Story Coverage

- `Default` for baseline usage
- `AllVariants` for semantic palette comparison
- `DarkVariants` for dark theme readability
